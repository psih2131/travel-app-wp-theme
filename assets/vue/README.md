# Vue-компоненты

Папка для Vue-компонентов темы.

- **TourCard.js** — карточка тура. Используется в `assets/js/all-tours-script.js` на странице архива туров. Подключается через `wp_enqueue_script` как `travel-tour-card` перед `travel-all-tours`.
